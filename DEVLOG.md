## DEVLOG

이 파일은 프로젝트 변경 이력을 기록하는 공식 문서입니다. 모든 코드 수정 및 기능 추가 시 아래 형식으로 항목을 추가해주세요.

---

### 템플릿

- 작업 날짜: YYYY-MM-DD
- 수정한 기능: (예: 카드 뒤집기 로직 수정)
- 수정 이유: (버그 수정 / 성능 개선 / 리팩토링 / 기능 추가)
- 변경된 파일 목록:
  - path/to/file.js
  - path/to/file.css
- 구현 내용: 간결하게 구현한 내용 요약 (의사결정, 알고리즘, API 변경 등)
- 테스트 결과: 수동 테스트/자동 테스트 결과 (브라우저, 해상도, 시나리오)
- 다음 작업: 후속 작업 또는 개선 예정사항

---

### 2026-03-09 — DEVLOG 초기 생성

- 작업 날짜: 2026-03-09
- 수정한 기능: DEVLOG.md 파일 추가
- 수정 이유: 변경 이력 기록을 위한 공식 문서 생성
- 변경된 파일 목록:
  - DEVLOG.md (추가)
- 구현 내용:
  - 프로젝트 루트에 `DEVLOG.md` 템플릿 파일을 추가하여 이후 모든 변경을 추적하도록 설정함.
  - 기록 항목은 날짜, 수정 기능, 이유, 변경 파일, 구현 내용, 테스트 결과, 다음 작업을 포함하도록 표준화함.
- 테스트 결과:
  - 문서 파일 생성 확인 (로컬 파일 시스템)
  - 앞으로 변경 시 이 형식으로 항목을 추가하면 감사 및 회귀 추적에 도움이 됨
- 다음 작업:
  - 향후 코드 수정 또는 기능 추가 시 본 파일에 항목을 추가
  - CI나 PR 템플릿에 DEVLOG 업데이트 항목을 권장으로 추가 고려

---

참고:
- DEVLOG에 기록된 내용은 변경 사항의 단위(커밋, PR, 작업 단위)에 맞춰 작성하세요.
- 가능한 경우 간단한 재현 단계(테스트 시나리오)를 `테스트 결과`에 포함하세요.

---

### 2026-03-09 — 인라인 핸들러 제거 및 저장소 안정화

- 작업 날짜: 2026-03-09
- 수정한 기능: 인라인 `onclick` 핸들러 제거, `app/storage.js` JSON 안전성 향상, UI 이벤트 바인딩 추가
- 수정 이유: `index.html`에 JS 로직(인라인 핸들러)이 포함되는 것을 제거하여 구조화 준수하고, 잘못된 로컬스토리지 데이터로 인한 예외 방지
- 변경된 파일 목록:
  - index.html (수정: inline onclick 제거, data-attributes 및 id 추가)
  - app/storage.js (수정: JSON.parse try/catch 및 setItem 에러 처리 추가)
  - js/game.js (수정: `bindUI()` 추가 — DOM 이벤트 바인딩)
- 구현 내용:
  - `index.html`에서 버튼의 `onclick` 속성을 제거하고, 테마 버튼에는 `data-theme`, 난이도 버튼에는 `data-difficulty`를 추가함. 모달 및 컨트롤 버튼에 `id`를 추가.
  - `app/storage.js`의 `saveRanking`을 try/catch로 감싸서 로컬스토리지에 잘못된 값이 있더라도 기본 구조로 복원하고 저장 실패시 콘솔에 에러를 남김.
  - `js/game.js`에 `bindUI()`를 추가하여 모든 UI 이벤트를 안전하게 바인딩하고, 기존 인라인 핸들러 기능을 대체함.
- 테스트 결과:
  - 로컬 파일 업데이트 완료 및 파일 시스템에 변경사항 반영됨.
  - 간단히 페이지를 열고 버튼이 동작하는 경우(수동) 정상 동작 예상 — (브라우저에서 확인 필요). 저장소에 비정상 데이터가 있어도 `saveRanking`이 예외를 던지지 않음.
- 다음 작업:
  - 브라우저에서 수동 테스트: 테마 선택, 난이도 선택(게임 시작), 랭킹 열기/닫기, 다시 시작, 메뉴 이동 확인
  - JS 런타임 콘솔 오류 검사 및 게임플레이 단계별 시나리오 검증 수행
  - iPad(1024x1366, 1180x820) UI 검증 및 필요 시 CSS 수정 제안

---

### 2026-03-09 — 메인 메뉴 좌우 레이아웃 변경 (Theme / Difficulty)

- 작업 날짜: 2026-03-09
- 수정한 기능: 메인 메뉴 레이아웃 — 테마 선택과 난이도 선택을 좌우 분할형으로 변경, 반응형 지원 추가
- 수정 이유: 모바일에서는 세로 스택, 태블릿/데스크톱에서는 좌우 분할 레이아웃을 사용하여 UI 활용도 향상
- 변경된 파일 목록:
  - index.html (수정: `.menu-container`, `.theme-section`, `.difficulty-section` 구조로 변경)
  - css/style.css (수정: 메뉴 레이아웃 및 반응형 스타일 추가, `.theme-card` 및 `.difficulty-btn` 조정)
- 구현 내용:
  - `index.html`에서 기존의 세로형 테마 및 난이도 블록을 `.menu-container`로 감싸 좌우 레이아웃을 구성함.
  - 테마 버튼에 `.theme-card` 클래스를 추가하여 정사각형 비율을 보장함(`aspect-ratio: 1 / 1`).
  - `.menu-container` CSS는 기본적으로 2열 그리드(1fr 1fr)를 사용하고, `@media (max-width:768px)`에서 1열로 전환되어 모바일에서 세로 스택으로 보이게 함.
  - 난이도 버튼의 패딩을 증가시켜 터치 대상 크기를 확보함.
- 테스트 결과:
  - 파일 수정 완료 및 스타일 추가 반영됨.
  - 개발 환경(파일 확인)에서 DOM 구조 변경 확인됨. 브라우저에서 실제 레이아웃 확인 권장 (아이패드/모바일 시뮬레이터 포함).
- 다음 작업:
  - 브라우저에서 아이패드 기준(1024x1366, 1180x820)과 모바일(<=768px)에서 레이아웃 테스트 및 카드 크기/보드 overflow 확인
  - 필요 시 `css/board.css`의 `#game-board` max-width/height와 카드 `max-width` 값 조정 제안


